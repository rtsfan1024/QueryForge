[pytest]
# 告诉 pytest 把当前目录(backend)加入路径，这样它才能看到 src 文件夹
pythonpath = .
testpaths = tests
# 注册 marker，避免 Warning
markers =
    integration: marks tests as integration tests
; [pytest]
; markers =
;     integration: real PostgreSQL integration tests